apiVersion: v1
kind: ConfigMap
metadata:
  name: hr-ai-config
  namespace: hr-ai
data:
  # Application configuration
  NODE_ENV: "production"
  PORT: "8080"
  
  # Add any additional configuration here
  # Example:
  # API_URL: "https://api.example.com"
  # LOG_LEVEL: "info"

