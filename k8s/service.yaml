apiVersion: v1
kind: Service
metadata:
  name: hr-ai-service
  namespace: hr-ai
  labels:
    app: hr-ai
spec:
  type: NodePort
  selector:
    app: hr-ai
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30080
    protocol: TCP
    name: http
  sessionAffinity: None

